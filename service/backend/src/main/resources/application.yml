spring:
  datasource:
    url: jdbc:postgresql://${db.host}/${db.name}
    username: ${db.user}
    password: ${db.pass}
  jooq:
    sql-dialect: POSTGRES
  servlet.multipart.enabled: false
server:
  port: 8080